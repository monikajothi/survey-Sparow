import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./styles.css"; // ✅ Import plain CSS

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
